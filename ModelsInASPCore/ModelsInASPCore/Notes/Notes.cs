
// ====================== 20 ==========================
/* What are Models  ? 
 * They are used to manage the application data
 * 
 * If we have to set and get the app data we can use of models
 * We are generating app data in controller using model class. But best practice is to use Repository Pattern and make class through it and access data source or data access layer through it. We write all logic there and then data hum apne controller main le kar ate hain 
 *  
 */

// ====================== 21 ==========================
/*
 * Every Model class represent table of database
 * DBContext is DataAccessLayer
 * Interface concept is use for Abstraction Layer. And why we use Abstraction ? 
 * To hide the unnecessary details from users 
 * 
 */